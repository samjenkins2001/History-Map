{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-azuretools.vscode-docker",
                "ms-vscode-remote.remote-containers",
                "ms-python.python"
            ]
        }
    },
    "postCreateCommand": "zsh -c 'source /etc/zsh/zshrc && nvm install node && nvm use node'"
}

